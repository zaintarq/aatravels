rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Package images — public read, signed-in admin uploads to their own folder
    match /packages/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
